#include <stdio.h>
#include <ncurses.h>

int main()
{
    initscr();  // start curses mode
    printw("Hello World!");
    refresh();  // prints to screen
    getch();    // gets user input
    endwin();

    return 0;
}
